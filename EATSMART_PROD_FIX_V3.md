# EatSmart prod fix v3

Fixes applied:
- Partial typed searches now resolve via Google Places Autocomplete when the user presses Search.
- Pressing Search no longer relies on the user selecting a suggestion.
- Blur no longer saves partial text like `victori` as the suburb.
- Street suggestion selection stores coords, suburb and city correctly.
- Existing security, photo endpoint, CORS, cuisine, result limit and add-price fixes retained.
